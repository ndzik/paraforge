{-# OPTIONS --safe #-}

module ParaForge where

-- These imports are intentionally minimal. They verify that the project is
-- connected to the standard library without committing the public API yet.
import Level
import Data.Product.Base
import Categories.Category
